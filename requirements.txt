# Core framework
Flask>=3.1.1
Werkzeug>=3.1.0

# PDF extraction — PyMuPDF preferred, pypdf as fallback
# Uncomment ONE of these depending on your environment:
PyMuPDF>=1.24.0          # Recommended: faster, better layout handling
# pypdf>=4.0.0           # Pure Python fallback — no native dependencies

# Environment configuration
python-dotenv>=1.1.0

# Production WSGI server
gunicorn>=22.0.0

# Background jobs (optional — app falls back to threads without Redis)
rq>=1.16.0
redis>=5.0.0

# Development / testing
pytest>=8.0.0
pytest-flask>=1.3.0
